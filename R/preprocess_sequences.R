#' Preprocess Sequences
#'
#' This function preprocesses and prepares biological sequences
#' 
#' 


preprocess_sequences <- function(sequences, ...) {
  
  
  for seq in sequences:
}






clean_sequence <- function(sequence, ...) {
  
}

compute_reverse_complement <- function(sequence, ...)