% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_tokens.R
\name{visualize_tokens}
\alias{visualize_tokens}
\title{Visualize Tokenized Sequence Statistics}
\usage{
visualize_tokens(statistics, top_n = 30, output_dir = NULL)
}
\arguments{
\item{statistics}{An object of class \code{"bioBPE_summary"} returned by
\code{summarize_tokens()}.}

\item{top_n}{Integer specifying the number of top tokens to display in
the top tokens plot. Default is 30.}

\item{output_dir}{Optional directory to save plots. If \code{NULL}, plots are
returned but not saved.}
}
\value{
A named list of plots:
\describe{
\item{\code{frequency_distribution}}{A line plot visualizing token rank
(log-scale) versus frequency (log-scale).}
\item{\code{top_tokens}}{A bar plot visualizing the most frequent tokens.}
\item{\code{cumulative_coverage}}{A line plot visualizing the cumulative
coverage of ranked tokens.}
}
}
\description{
Generates visual summaries of tokenized biological sequences, including
token frequency distribution, top tokens, and cumulative coverage.
}
\details{
The visualization step includes three plots. The exact operations
are delegated to the following functions:
\itemize{
\item \code{plot_token_frequency_distribution} to visualize token frequency
\item \code{plot_top_tokens} to visualize the top N tokens
\item \code{plot_cumulative_coverage} to visualize the cumulative coverage
across token ranks
}
}
\examples{
\dontrun{
   # Generate simulated data
   data <- generate_data(
       n          = 3, 
       length     = 1000, 
       vocab_size = 25, 
       preprocess = TRUE,
       annotate   = TRUE,
       tokenize   = TRUE,
       summarize  = TRUE,
       verbose    = FALSE
   )
   
   # Visualize the tokenized sequences
   dna_plots <- visualize_tokens(statistics = data$dna_summary,
                                 top_n = 30,
                                 output_dir = "output")
   rna_plots <- visualize_tokens(statistics = data$rna_summary,
                                 top_n = 30,
                                 output_dir = "output")
   ana_plots <- visualize_tokens(statistics = data$aa_summary,
                                 top_n = 30,
                                 output_dir = "output")
}

}
\seealso{
Other visualization: 
\code{\link{plot_cumulative_coverage}()},
\code{\link{plot_token_frequency_distribution}()},
\code{\link{plot_top_tokens}()}
}
\concept{visualization}
\keyword{visualization}
