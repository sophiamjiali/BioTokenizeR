% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_sequences.R
\name{preprocess_sequences}
\alias{preprocess_sequences}
\title{Preprocess Biological Sequences for BPE Tokenization}
\usage{
preprocess_sequences(seqs)
}
\arguments{
\item{seqs}{A \code{Biostrings::XStringSet} object containing DNA, RNA, or AA
sequences (e.g. \code{DNAStringSet}, \code{RNAStringSet}, or \code{AAStringSet}).}
}
\value{
An object of class \code{"bioBPE_preprocessed"}, being a list containing:
\describe{
\item{\code{seqs}}{A cleaned \code{Biostrings::XStringSet} object of
preprocessed sequences.}
\item{\code{type}}{A string indicating the biological sequence type:
\code{"DNA"}, \code{"RNA"}, or \code{"AA"}.}
\item{\code{preproc_steps}}{A record of preprocessing steps applied.}
\item{\code{annot_steps}}{Annotation steps applied (initialized as \code{NULL}).}
}
}
\description{
Performs input validation and standardized preprocessing of biological
sequences (DNA, RNA, or AA) to ensure compatibility with downstream byte-pair
encoding (BPE) tokenization and analysis workflows.
}
\details{
This function verifies that the input is a valid \code{Biostrings::XStringSet}
object, applies preprocessing appropriate to the sequence type, and returns
a structured object of class \code{"bioBPE_preprocessed"} containing the processed
sequences and metadata describing the preprocessing steps applied.

The preprocessing step may include sequence cleaning, normalization,
or removal of invalid characters, depending on sequence type. The exact
operations are delegated to internal helper functions:
\itemize{
\item \code{.BioTokenizeR_preprocess_DNA()} for DNA sequences
\item \code{.BioTokenizeR_preprocess_RNA()} for RNA sequences
\item \code{.BioTokenizeR_preprocess_AA()} for amino acid sequences
}
}
\examples{
\dontrun{
   # Generate simulated data
   data <- generate_data(
       n          = 3, 
       length     = 1000, 
       vocab_size = 25, 
       preprocess = FALSE,
       annotate   = FALSE,
       tokenize   = FALSE,
       summarize  = FALSE,
       verbose    = FALSE
   )

   # Preprocess the sequences
   dna_preproc <- preprocess_sequences(seqs = data$dna_seq)
   rna_preproc <- preprocess_sequences(seqs = data$rna_seq)
   aa_preproc <- preprocess_sequences(seqs = data$aa_seq)
}

}
\references{
{
Pagès H, Aboyoun P, Gentleman R, DebRoy S (2025). Biostrings:
Efficient manipulation of biological strings.
doi:10.18129/B9.bioc.Biostrings https://doi.org/10.18129/B9.bioc.Biostrings,
R package version 2.78.0, https://bioconductor.org/packages/Biostrings.
}
}
\seealso{
Other preprocessing: 
\code{\link{.BioTokenizeR_compute_bio_score}()},
\code{\link{.BioTokenizeR_normalize}()},
\code{\link{.BioTokenizeR_preprocess_AA}()},
\code{\link{.BioTokenizeR_preprocess_DNA}()},
\code{\link{.BioTokenizeR_preprocess_RNA}()}
}
\concept{preprocessing}
\keyword{internal}
\keyword{preprocessing}
